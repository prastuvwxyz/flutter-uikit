name: utils
description: Pure Dart utility functions and extensions for the rental fleet portal
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  # Pure Dart dependencies only
  intl: ^0.19.0

dev_dependencies:
  test: ^1.26.2
  very_good_analysis: ^6.0.0

resolution: workspace
