name: tokens
description: Design tokens package providing color, typography, spacing, and other design primitives
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.26.2

flutter:
  uses-material-design: true

resolution: workspace