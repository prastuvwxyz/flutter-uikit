name: ui_templates
publish_to: none
description: Minimal UI templates (dashboard scaffold) for flutter-uikit
version: 0.0.1
environment:
  sdk: ">=2.17.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  ui_components:
    path: ../ui_components
