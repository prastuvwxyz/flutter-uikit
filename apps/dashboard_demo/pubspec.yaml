name: dashboard_demo
description: Demo app to run the DashboardTemplate on web
publish_to: none
version: 0.0.1
environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  ui_templates:
    path: ../../packages/ui_templates
  ui_components:
    path: ../../packages/ui_components

flutter:
  uses-material-design: true
